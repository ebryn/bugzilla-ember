<article class="attachment-main">
  <h1>{{description}}</h1>

  {{#if isDeleted}}
    Attachment was deleted
  {{else}}
    {{#if isText}}
      <pre><code>{{decodedData}}</code></pre>
    {{else}}
      <img {{bindAttr src="dataUri"}}>
    {{/if}}
  {{/if}}
</article>