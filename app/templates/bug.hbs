{{#if errorMessage}}
  <div class="flash">{{errorMessage}}</div>
{{else}}
  {{#if isLoading}}
    Loading...
  {{/if}}

  <div {{bindAttr class=":bug isLoading:hide"}}>
    {{outlet}}
    {{partial 'bug/attachments'}}
    {{partial 'bug/comments'}}
  </div>

  {{outlet 'modal'}}
{{/if}}